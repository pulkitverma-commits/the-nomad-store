// Same object card on both networks. Without this file Twitter falls back to
// the site-wide picture and the object being shared never appears.
export { default, alt, size, contentType } from './opengraph-image';
