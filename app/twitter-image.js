// The share card is the same picture on both networks — Twitter just wants its
// own route. Everything lives in the opengraph card next door.
export { default, alt, size, contentType } from './opengraph-image';
